import { test } from "@playwright/test";

test.describe("Feature CRUD", () => {
  test.skip("Feature erstellen: + Button → Form → Speichern → erscheint in Liste", async () => {});
  test.skip("Feature öffnen: Doppelklick → navigiert zu /features/:id", async () => {});
  test.skip("Feature bearbeiten: Titel + RTF-Inhalt → Speichern → Änderung sichtbar", async () => {});
  test.skip("Feature löschen: Delete → Confirm → verschwindet", async () => {});
  test.skip("Projekt verknüpfen im Projekte-Tab → Speichern → bleibt verknüpft", async () => {});
  test.skip("Use Case erstellen im UC-Tab → erscheint in UC-Liste", async () => {});
  test.skip("Board / Listen Toggle in Feature-Übersicht", async () => {});
});
