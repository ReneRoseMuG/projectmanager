# Log: text-muted Analyse

**Datum:** 22.05.26  
**Schritt:** 1 — text-muted analysieren  
**Status:** ✅ Abgeschlossen

## Was wurde umgesetzt

Die Vorkommen von `text-muted` in `apps/web/src` wurden vollständig gesucht und als Freigabeliste erfasst. Es wurden noch keine `text-muted`-Stellen ersetzt oder entfernt, weil der Auftrag hierfür eine Zwischenabstimmung verlangt. Die Fundstellen liegen in Admin-Seiten, `PageHeader`, `ProjectMilestoneFilterBar` und `SetupPasswordPage`. Die übrigen Designpunkte können unabhängig davon weiter umgesetzt werden.

## Geänderte / angelegte Dateien

| Datei | Art | Kurzbeschreibung |
|---|---|---|
| `logs/2026-05-22-schritt-01-text-muted-analyse.md` | neu | Schritt-Log zur vollständigen `text-muted`-Fundstellenanalyse |
| `logs/README.md` | geändert | Log-Übersicht um den neuen Schritt ergänzt |

## Probleme und Abweichungen

`text-muted` bleibt bis zur Freigabe unverändert. Dadurch kann das Gesamtziel erst nach der ausstehenden Entscheidung vollständig abgeschlossen werden.

## Offene Punkte / Folgeaufgaben

Freigabe des Auftraggebers erforderlich: `text-muted` entweder durch `text-steel-500` ersetzen oder die jeweiligen Texte/Props entfernen.
