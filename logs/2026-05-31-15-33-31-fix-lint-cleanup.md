# Log: Lint Cleanup

**Datum:** 31.05.26  
**Uhrzeit:** 15:33:31  
**Schritt:** Fix — Lint Cleanup  
**Status:** ✅ Abgeschlossen

## Was wurde umgesetzt

Die beauftragten Lintfehler aus den ersten beiden Gruppen wurden bereinigt. Im Web wurden ungenutzte Variablen und Imports in Formular- und Wiki-Komponenten entfernt. In der API wurden ungenutzte Imports, ein ungenutzter Helper, ein ungenutzter Proxy-Parameter sowie ein leerer Snapshot-Parameter entfernt. Der `tasks`-Import in `day-plan.service.ts` wurde in einen reinen Typimport aufgeteilt, damit kein unnötiger Runtime-Import entsteht.

## Geänderte / angelegte Dateien

| Datei | Art | Kurzbeschreibung |
|---|---|---|
| `apps/web/src/components/backlog/BacklogItemForm.tsx` | geändert | Ungenutztes `selectedFeature` und `useMemo` entfernt |
| `apps/web/src/components/milestones/MilestoneForm.tsx` | geändert | Ungenutzten `StatusToggle`-Import entfernt |
| `apps/web/src/pages/WikiPage.tsx` | geändert | Ungenutzten Submit-Parameter entfernt |
| `apps/api/src/repositories/journal.repository.ts` | geändert | Ungenutzte Imports entfernt |
| `apps/api/src/services/auth.service.ts` | geändert | Ungenutzten `sql`-Import entfernt |
| `apps/api/src/services/backlog.service.ts` | geändert | Ungenutzten Helper entfernt |
| `apps/api/src/services/day-plan.service.ts` | geändert | `tasks` auf Typimport umgestellt |
| `apps/api/src/services/dump.service.ts` | geändert | Ungenutzte Parameter und ungenutzten Helper entfernt |
| `logs/2026-05-31-15-33-31-fix-lint-cleanup.md` | neu | Schritt-Log für diesen Fix |
| `logs/README.md` | geändert | Log-Index aktualisiert |

## Probleme und Abweichungen

`npm run lint -w apps/web` schlägt weiterhin mit dem nicht beauftragten Restfehler `_statusSortOrder` in `useStatusCascadeWorkflow.tsx` fehl. `npm run lint -w apps/api` schlägt weiterhin mit dem nicht beauftragten Hook-Namenskonflikt `useCaseParentContexts` in `use-cases.service.ts` fehl. `npm run build` läuft erfolgreich durch; Vite meldet nur die bestehende Chunk-Size-Warnung.

## Offene Punkte / Folgeaufgaben

Die beiden bewusst nicht gelösten Restfehler können in einem separaten Fix behandelt werden.
